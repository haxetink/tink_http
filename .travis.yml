sudo: required
dist: trusty

language: node_js
node_js: 6

os:
  - linux
      
install:
  - npm install

script:
  - npm run --silence haxe -- master.hxml -D containers=node -D targets=node,neko
  - npm run --silence haxe -- master.hxml -D containers=php -D targets=node
  # - npm run --silence haxe -- master.hxml -D containers=neko-tools -D targets=node # does not support PATCH/PUT/DELETE
  # - npm run --silence haxe -- master.hxml -D containers=node -D targets=cpp # haxeshim not working with cpp yet
  # - npm run --silence haxe -- master.hxml -D containers=node -D targets=js # why DELETE on travix does not send the body?
  # - npm run --silence haxe -- master.hxml -D containers=tcp -D targets=node -lib tink_tcp -lib tink_runloop
